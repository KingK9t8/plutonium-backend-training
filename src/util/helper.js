const s=new Date
// console.log(s);


const currentDate=s.getDate()

const currentMonth=s.getMonth()

const batchInfo=()=> console.log("Plutonium, W3D5, the topic for today is NodeJs module system");

module.exports={currentDate,currentMonth,batchInfo}
